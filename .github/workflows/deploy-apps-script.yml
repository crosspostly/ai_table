name: Deploy to Google Apps Script

on:
  push:
    branches:
      - web-interface-with-design
    paths:
      - 'table/**'
      - '.clasp.json'
      - '.github/workflows/deploy-apps-script.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install clasp
        run: npm install -g @google/clasp

      - name: Setup clasp credentials
        run: echo "${{ secrets.CLASPRC_JSON }}" > ~/.clasprc.json

      - name: Deploy to Apps Script
        run: |
          echo "📦 Deploying to Apps Script..."
          echo "📋 Script ID: 15tMLr3G3kbBKjMVi7r2XqPR3kBSWO0jCDYHijIhy9ko2rsIceG8tLoYa"
          clasp push -f
          echo "✅ Deployment complete!"

      - name: Summary
        run: |
          echo "🎉 Deployment completed successfully!"
          echo "📋 Deployed: table/server/, table/client/, table/shared/, table/web/"
          echo "📋 Project: 15tMLr3G3kbBKjMVi7r2XqPR3kBSWO0jCDYHijIhy9ko2rsIceG8tLoYa"
