name: Deploy to Google Apps Script

on:
  push:
    branches:
      - web-interface-with-design
    paths:
      - 'table/**'
      - '.clasp-*.json'
      - '.github/workflows/deploy-apps-script.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install clasp
        run: npm install -g @google/clasp

      - name: Setup clasp credentials
        run: echo "${{ secrets.CLASPRC_JSON }}" > ~/.clasprc.json

      - name: Deploy Server Project
        run: |
          echo "📦 Deploying Server project..."
          cp .clasp-server.json .clasp.json
          cp .claspignore-server table/.claspignore
          clasp push -f
          echo "✅ Server deployment complete!"

      - name: Deploy Client Project
        run: |
          echo "📦 Deploying Client project (Container-bound to Google Sheets)..."
          cp .clasp-client.json .clasp.json
          cp .claspignore-client table/.claspignore
          clasp push -f
          echo "✅ Client deployment complete!"

      - name: Summary
        run: |
          echo "🎉 All deployments completed successfully!"
          echo "📋 Server: table/server/ + table/shared/ → Script ID 15tMLr3G3kbBKjMVi7r2XqPR3kBSWO0jCDYHijIhy9ko2rsIceG8tLoYa"
          echo "📋 Client: table/client/ + table/shared/ + table/web/ → Script ID 1DdlYfvo0EfEA1O1nb5DRI0o-WJoIivtfIPNSE1C1bt3IvvWC91sGE6Xs"
